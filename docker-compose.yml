version: '3.8'

services:
  postgres:
    image: postgres
    container_name: vacancy
    environment:
      POSTGRES_DB: vacancy
      POSTGRES_USER: aboba
      POSTGRES_PASSWORD: aboba
    ports:
      - "5432:5432"
    restart: no

